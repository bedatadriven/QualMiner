---
title: "Text analysis"
author: "Metin Yazici, BeDataDriven"
date: "`r Sys.Date()`"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

In that section, we take text as data.

## Textual data processing

Perform stemming, which you bring nouns/verbs back to infinitive forms, and tokenization, which is separating words in meaningful pieces.

Remove noise in the text. There are usually a list of such words for each language and they are called as stop-words as they are overly distributed in the text and they will not give so meaningful results itself. Stop-words are including articles (*el/la*), conjunctions (*y*), pronouns (*yo/tú/etc.*) and so on.

